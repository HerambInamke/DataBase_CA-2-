const express = require('express');
const gymworkout = require('../models/workout');

const router = express.Router();

app.post('/',router);

app.get('/workout', router );
app.put('/workout,', router );
